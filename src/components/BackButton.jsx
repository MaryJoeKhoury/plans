import React from "react";

const BackButton = () => {
  return (
    <div>
      <button type="button" className="back-button arrow">
        Back
      </button>
    </div>
  );
};

export default BackButton;
